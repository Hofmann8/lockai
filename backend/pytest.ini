[pytest]
testpaths = tests
# _tmp_runtime holds runtime model registries written by the tests themselves.
# On Windows its pytest-temp subdir is unreadable and breaks collection, so skip it.
# (ASCII only: iniconfig reads this file with the system locale encoding.)
norecursedirs = _tmp_runtime _tmp_runtime_local .git __pycache__
